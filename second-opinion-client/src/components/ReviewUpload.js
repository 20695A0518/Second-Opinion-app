import React from 'react';

const ReviewUpload = () => {
  return (
    <div>
      <h2>Upload Review</h2>
      <form>
        <input type="file" />
        <button type="submit">Upload</button>
      </form>
    </div>
  );
};

export default ReviewUpload;
